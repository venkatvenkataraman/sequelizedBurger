<h1>Eat-Da-Burgers!</h1>

<img src="assets/img/burger.png" alt="Burger Image" height="250" width=auto>

<h2>Burgers that are available to eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="0" checked> Not Devoured!<br>
    <input type="radio" name="devoured" value="1"> Devoured!
  </div>

  <button type="submit">Add Burger</button>
</form>
